import React from 'react'
import './input.scss'
import Button from "react-bootstrap/Button";

const Save = () => {
  return (
    <div className="input-wrapper">
      <Button className="button" variant="primary" type="button">save</Button>
    </div>
  );
}

export default Save
